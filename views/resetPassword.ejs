<!DOCTYPE html>
<html lang="en">
<%- include('head') %>
<body class="bg-gray-200">
    <section>
        <%- include('projectSummary') %>
    </section>

    <section>
        <div class="max-w-4xl mx-auto text-center mt-4 -mb-4">
        <% if(errorMsg) { %>
            <div class="p-2 bg-slate-200 border-2 rounded-lg">
                <div class="font-bold text-red-400"> <%= errorMsg %> </div>
            </div>
        <% } %>
        </div>
    </section>

    <section>
        <%- include('./forms/passwordResetForm') %>        
    </section>
</body>
</html>