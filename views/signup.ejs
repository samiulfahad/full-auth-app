<!DOCTYPE html>
<html lang="en">
<%- include('head') %>
<body class="bg-gray-200">
    <section>
        <%- include('projectSummary') %>
    </section>
    <section>
        <div class="max-w-4xl mx-auto text-center mt-4">
        <% if(errorMsg || successMsg) { %>
            <div class="p-2 bg-slate-200 border-2 rounded-lg">
                <div class="font-bold text-green-400"> <%= successMsg %> </div>
                <div class="font-bold text-red-400"> <%= errorMsg %> </div>
            </div>
        <% } %>
        </div>
        <div class="-mt-8">
            <%- include('./forms/signupForm') %> 
        </div>
    </section>
</body>
</html>